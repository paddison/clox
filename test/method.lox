class Scone {
  topping(first, second) {
    print this;
    print "scone with " + first + " and " + second;
  }
}

fun main() {
  var scone = Scone();
  var blone = Scone();
  scone.topping("berries", "cream");
}

main();
